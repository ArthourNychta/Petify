<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Petify">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Petify">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapPage.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity android:name=".gettoknowyourpagemenu.Health_ArticlePage" />
        <activity android:name=".gettoknowyourpagemenu.ArticleDetail" />
        <activity android:name=".gettoknowyourpagemenu.Food_ArticlePage" />
        <activity android:name=".Login.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register.Register" />
        <activity android:name=".gettoknowyourpagemenu.Gettoknowyourpet_Menu" />
        <activity android:name=".DogFoodPage.CatFoodPage" />
        <activity android:name=".DogAccessories.CatAccessories" />
        <activity android:name=".DogAccessories.DogAccessories" />
        <activity android:name=".PetAccessoriesSearch.PetAccessories" />
        <activity android:name=".PaymentSuccess.PaymentSuccess" />
        <activity android:name=".DogFoodPage.DogFoodPage" />
        <activity android:name=".PetFoodSearch.PetFoodSearch_Page" /> <!-- <activity android:name=".HomePage.HomePage" /> -->
        <activity
            android:name=".HomePage.HomePage"
            android:label="@string/app_name" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>